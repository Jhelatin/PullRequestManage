# Сервис назначения ревьюеров для Pull Request'ов

Микросервис на Go для автоматического назначения ревьюеров на Pull Request'ы, а также управления командами и пользователями в рамках тестового задания для стажёра Backend (осенняя волна 2025).

## 🚀 Основные возможности

*   **Управление командами и пользователями**: Создание команд с участниками, обновление статуса активности пользователей.
*   **Автоматическое назначение ревьюеров**: При создании Pull Request сервис автоматически назначает до двух активных ревьюеров из команды автора.
*   **Жизненный цикл PR**: Создание, слияние (Merge) и переназначение ревьюеров для открытых PR.
*   **Идемпотентность**: Ключевые операции, такие как слияние PR, идемпотентны.
*   **API на основе контракта**: Взаимодействие с сервисом происходит исключительно через HTTP API, описанное в файле `openapi.yaml`.

## 🛠️ Стек технологий

*   **Язык**: Go 1.25
*   **База данных**: PostgreSQL
*   **Веб-фреймворк**: Gin-Gonic
*   **Взаимодействие с БД**: pgx/v5
*   **Окружение**: Docker, Docker Compose
*   **Миграции**: migrate/migrate

## ⚙️ Предварительные требования

Для запуска и работы с проектом вам понадобятся:
*   **Docker** и **Docker Compose**
*   **Make** (для удобного использования команд)
*   **Go** (для локальной разработки и запуска тестов)

## 🏁 Инструкция по запуску

### 1. Клонирование репозитория

```bash
git clone <URL вашего репозитория>
cd <название-папки-проекта>
```

### 2. Конфигурация

Проект использует `.env` файл для конфигурации. Создайте его, скопировав шаблон `.env.example`:
```bash
cp .env.example .env
```
Файл `.env` по умолчанию настроен для запуска всего приложения внутри Docker.

### 3. Запуск сервиса (рекомендуемый способ)

Самый простой способ поднять всё окружение — использовать `Makefile`. Эта команда соберет Go-приложение, поднимет контейнеры с приложением и базой данных, а также автоматически применит миграции.

```bash
make up
```После выполнения команды сервис будет доступен по адресу `http://localhost:8080`.

Вы можете проверить логи, чтобы убедиться, что все запустилось корректно:
```bash
make logs
```
Вы должны увидеть строку `INFO: Starting server on http://localhost:8080`.

### 4. Остановка сервиса

Для остановки всех контейнеров и удаления созданных сетей и данных выполните:
```bash
make down
```

### 5. Управление миграциями БД

Для ручного управления миграциями можно использовать следующие команды:
*   `make migrate-up`: Применить все новые миграции.
*   `make migrate-down`: Откатить последнюю примененную миграцию.

## ✅ Тестирование

файл 000002_seed_test_data.up.sql создаёт пользователей для тестов.

---

## ❓ Вопросы, проблемы и принятые решения

В ходе разработки были приняты следующие архитектурные и технические решения.

### 1. Архитектура: Слоистая (Clean Architecture)
**Решение:** Применена классическая слоистая архитектура:
*   **`Handler` (Обработчик):** Отвечает только за HTTP: парсинг запросов, валидацию и формирование ответов. Не содержит бизнес-логики.
*   **`Service` (Сервис):** Содержит всю бизнес-логику и правила (например, "нельзя менять смерженный PR", "выбрать двух случайных ревьюеров").
*   **`Repository` (Репозиторий):** Отвечает за взаимодействие с базой данных. Предоставляет простые CRUD-подобные методы.
*   **`app.go` (Сборщик):** Выполняет роль DI-контейнера — инициализирует все слои и "склеивает" их вместе, передавая зависимости через конструкторы.

### 2. Надежность: Graceful Shutdown
**Проблема:** При остановке сервиса (например, командой `docker-compose down` или при перезапуске в Kubernetes) важно не обрывать текущие запросы, чтобы избежать потери данных.

**Решение:** В `app.go` реализован механизм Graceful Shutdown. При получении сигнала `SIGINT` или `SIGTERM` сервер перестает принимать новые соединения, но дает несколько секунд на завершение уже начатых запросов, и только потом останавливается. Это является стандартом для любого production-ready сервиса.

### 3. Ответственность за уникальность id
**Проблема:** openapi предписывает передавать prID при создании, хотя ответственность за уникальность id обычно не ложится на пользователя
**Решение:** продолжать следовать openapi, т.к. могут быть ситуации, когда за этим следит внешний сервис, а наш сервис лишь должен выполнять функционал учёта команд и назначение ревьюверов